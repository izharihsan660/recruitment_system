import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Badge, Button, Card, EmptyState, PageHeader, SelectInput, TextInput } from '@/Components/shared/ui';
import { CandidateSource, humanize, Paginated, rows, TalentPoolItem } from '@/lib/recruitment';
import { Head, Link } from '@inertiajs/react';
export default function TalentPoolIndex({ talentPools, candidateSources }: { talentPools: Paginated<TalentPoolItem>; candidateSources: CandidateSource[] }): JSX.Element { return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Talent Pool</h1>}><Head title="Talent Pool" /><PageHeader title="Talent Pool" description="Database kandidat reusable untuk lowongan aktif." /><Card className="mb-4 p-4"><div className="grid gap-3 md:grid-cols-4"><TextInput placeholder="Search nama, skill, posisi" /><SelectInput><option>Status</option></SelectInput><SelectInput><option>Source</option>{candidateSources.map((source) => <option key={source.id}>{source.name}</option>)}</SelectInput><TextInput placeholder="Tags" /></div></Card><Card className="overflow-hidden"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-4">Nama</th><th className="p-4">Email</th><th className="p-4">Status</th><th className="p-4">Tags</th><th className="p-4">Source</th><th className="p-4 text-right">Aksi</th></tr></thead><tbody className="divide-y">{rows(talentPools).map((item) => <tr key={item.id}><td className="p-4 font-medium">{item.candidate?.name}</td><td className="p-4">{item.candidate?.email}</td><td className="p-4"><Badge tone="blue">{humanize(item.status)}</Badge></td><td className="p-4">{item.tags?.join(', ')}</td><td className="p-4">Manual</td><td className="p-4 text-right"><Link href={`/hr/talent-pool/${item.id}`}><Button variant="secondary">Detail</Button></Link></td></tr>)}</tbody></table>{rows(talentPools).length === 0 && <div className="p-4"><EmptyState title="Talent pool kosong" /></div>}</Card></AuthenticatedLayout>; }
