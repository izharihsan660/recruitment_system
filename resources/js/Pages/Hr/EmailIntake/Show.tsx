import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Badge, Button, Card, FormLabel, PageHeader, SelectInput, TextArea } from '@/Components/shared/ui';
import { EmailIntake, formatDate, humanize, JobPosting } from '@/lib/recruitment';
import { Head, router, useForm } from '@inertiajs/react';
export default function EmailIntakeShow({ email, jobPostings }: { email: EmailIntake; jobPostings: JobPosting[] }): JSX.Element { const form = useForm({ job_posting_id: '', consent: false, notes: '', reason: '' }); return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Detail Email</h1>}><Head title="Detail Email" /><PageHeader title={email.subject ?? 'Email'} description={`${email.sender_email ?? '-'} • ${formatDate(email.received_at)}`} /><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><Card className="p-6"><div className="mb-4 flex gap-2"><Badge tone={email.is_duplicate ? 'red' : 'blue'}>{email.is_duplicate ? 'Duplicate' : humanize(email.status)}</Badge></div><p className="whitespace-pre-line text-sm text-slate-700">{email.body}</p>{email.attachment_path && <a className="mt-4 block text-sm text-blue-600" href={email.attachment_path}>Download Attachment CV</a>}</Card><Card className="p-6"><h2 className="mb-4 font-semibold">Actions</h2><div className="space-y-4"><div><FormLabel>Assign ke Lowongan</FormLabel><SelectInput value={form.data.job_posting_id} onChange={(e) => form.setData('job_posting_id', e.target.value)}><option value="">Pilih lowongan</option>{jobPostings.map((job) => <option key={job.id} value={job.id}>{job.position_name}</option>)}</SelectInput></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.data.consent} onChange={(e) => form.setData('consent', e.target.checked)} /> Consent</label><Button onClick={() => router.post(`/hr/email-intake/${email.id}/assign-to-job`, { job_posting_id: Number(form.data.job_posting_id), consent: form.data.consent })}>Assign ke Lowongan</Button><TextArea rows={3} placeholder="Catatan talent pool" value={form.data.notes} onChange={(e) => form.setData('notes', e.target.value)} /><Button variant="secondary" onClick={() => router.post(`/hr/email-intake/${email.id}/move-to-talent-pool`, { notes: form.data.notes, consent: form.data.consent })}>Pindah ke Talent Pool</Button><TextArea rows={3} placeholder="Alasan reject" value={form.data.reason} onChange={(e) => form.setData('reason', e.target.value)} /><div className="grid grid-cols-3 gap-2"><Button variant="danger" onClick={() => router.post(`/hr/email-intake/${email.id}/reject`, { reason: form.data.reason })}>Reject</Button><Button variant="secondary" onClick={() => router.post(`/hr/email-intake/${email.id}/ignore`)}>Ignore</Button><Button variant="secondary" onClick={() => router.post(`/hr/email-intake/${email.id}/spam`)}>Spam</Button></div></div></Card></div></AuthenticatedLayout>; }
