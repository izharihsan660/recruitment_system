import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Badge, Button, Card, FormLabel, PageHeader, SelectInput, TextInput } from '@/Components/shared/ui';
import { Head, router, useForm } from '@inertiajs/react';
import { FormEvent, useState } from 'react';
interface Smtp { id: number; host: string; port: number; username: string; encryption?: string | null; from_address: string; from_name: string; is_active: boolean }
export default function Smtp({ smtpSettings }: { smtpSettings: Smtp[] }): JSX.Element { const current = smtpSettings[0]; const [result, setResult] = useState(''); const form = useForm({ host: current?.host ?? '', port: String(current?.port ?? 587), username: current?.username ?? '', password: '', encryption: current?.encryption ?? 'tls', from_address: current?.from_address ?? '', from_name: current?.from_name ?? '', is_active: current?.is_active ?? true }); function submit(event: FormEvent): void { event.preventDefault(); const payload = { ...form.data, port: Number(form.data.port) }; current ? router.put(`/admin/smtp-settings/${current.id}`, payload, { preserveScroll: true }) : router.post('/admin/smtp-settings', payload, { preserveScroll: true }); } function test(): void { if (!current) { setResult('Simpan konfigurasi dulu sebelum test koneksi.'); return; } window.axios.post(`/admin/smtp-settings/${current.id}/test-connection`, { email: form.data.from_address }).then(() => setResult('Test koneksi berhasil.')).catch(() => setResult('Test koneksi gagal. Periksa konfigurasi SMTP.')); } return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">SMTP</h1>}><Head title="SMTP" /><PageHeader title="SMTP Setting" description="Konfigurasi email keluar recruitment system." /><Card className="p-6"><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><div><FormLabel>Host</FormLabel><TextInput value={form.data.host} onChange={(e) => form.setData('host', e.target.value)} /></div><div><FormLabel>Port</FormLabel><TextInput value={form.data.port} onChange={(e) => form.setData('port', e.target.value)} /></div><div><FormLabel>Username</FormLabel><TextInput value={form.data.username} onChange={(e) => form.setData('username', e.target.value)} /></div><div><FormLabel>Password</FormLabel><TextInput type="password" value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} placeholder="Masked" /></div><div><FormLabel>Encryption</FormLabel><SelectInput value={form.data.encryption} onChange={(e) => form.setData('encryption', e.target.value)}><option value="">None</option><option value="tls">TLS</option><option value="ssl">SSL</option><option value="smtp">SMTP</option><option value="smtps">SMTPS</option></SelectInput></div><div><FormLabel>From Address</FormLabel><TextInput value={form.data.from_address} onChange={(e) => form.setData('from_address', e.target.value)} /></div><div><FormLabel>From Name</FormLabel><TextInput value={form.data.from_name} onChange={(e) => form.setData('from_name', e.target.value)} /></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.data.is_active} onChange={(e) => form.setData('is_active', e.target.checked)} /> Status aktif</label><div className="md:col-span-2 flex flex-wrap items-center justify-end gap-2"><Button type="button" variant="secondary" onClick={test}>Test Koneksi</Button><Button disabled={form.processing}>Simpan</Button></div>{result && <div className="md:col-span-2"><Badge tone={result.includes('berhasil') ? 'green' : 'red'}>{result}</Badge></div>}</form></Card></AuthenticatedLayout>; }
