import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, router, useForm } from '@inertiajs/react';

export default function Show({ employee, checklist, users }: { employee: any; checklist: any; users: any[] }): JSX.Element { const itemForm = useForm({ title: '', description: '' }); const days = Math.ceil((new Date(checklist.first_day).getTime() - Date.now()) / 86400000); return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Pre-boarding</h1>}><Head title="Pre-boarding" /><div className="space-y-4"><div className="rounded-lg border bg-white p-4"><p className="font-semibold">{employee.full_name}</p><p>Status: {checklist.status} · First day dalam {days} hari</p><div className="mt-2 h-2 rounded bg-slate-100"><div className="h-2 rounded bg-blue-600" style={{ width: `${(checklist.items.filter((i: any) => i.status === 'done').length / Math.max(checklist.items.length, 1)) * 100}%` }} /></div></div><form onSubmit={(e) => { e.preventDefault(); itemForm.post(`/hr/preboarding/${checklist.id}/items`); }} className="flex gap-2"><input className="rounded border p-2" placeholder="Tambah item" onChange={(e) => itemForm.setData('title', e.target.value)} /><button className="rounded bg-blue-600 px-3 text-white">Tambah Item</button></form>{checklist.items.map((item: any) => <div key={item.id} className="rounded-lg border bg-white p-4"><div className="flex items-center justify-between"><div><p className="font-medium">{item.title}</p><p className="text-sm text-slate-500">PIC: {item.pic?.name ?? '-'}</p></div><span>{item.status}</span></div><div className="mt-3 flex gap-2"><select className="rounded border p-2" onChange={(e) => router.post(`/hr/preboarding/items/${item.id}/assign`, { assigned_to: e.target.value })}><option>Pilih PIC</option>{users.map((user) => <option key={user.id} value={user.id}>{user.name}</option>)}</select><button className="rounded bg-green-600 px-3 text-white" onClick={() => router.post(`/hr/preboarding/items/${item.id}/complete`)}>Selesai</button><button className="rounded bg-red-600 px-3 text-white" onClick={() => confirm('Hapus item?') && router.delete(`/hr/preboarding/items/${item.id}`)}>Hapus</button></div></div>)}</div></AuthenticatedLayout>; }
