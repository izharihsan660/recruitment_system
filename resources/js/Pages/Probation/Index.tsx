import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, Link } from '@inertiajs/react';

export default function Index({ employees }: { employees: any }): JSX.Element { const rows = employees.data ?? employees; return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Probation</h1>}><Head title="Probation" /><div className="rounded-lg border bg-white"><table className="w-full text-sm"><tbody>{rows.map((employee: any) => <tr key={employee.id} className="border-b"><td className="p-3"><Link className="font-medium text-blue-600" href={`/hr/probation/${employee.id}`}>{employee.full_name}</Link><p className="text-slate-500">{employee.position_name}</p></td><td className="p-3">{employee.department?.name}</td><td className="p-3">{employee.probation_record?.day30_due ?? '-'}</td><td className="p-3">{employee.probation_record?.status ?? 'Belum dibuat'}</td></tr>)}</tbody></table></div></AuthenticatedLayout>; }
