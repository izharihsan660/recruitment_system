import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, Link } from '@inertiajs/react';

export default function Index({ probations }: { probations: any }): JSX.Element { const rows = probations.data ?? probations; return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Probation</h1>}><Head title="Probation" /><div className="rounded-lg border bg-white"><table className="w-full text-sm"><tbody>{rows.map((record: any) => <tr key={record.id} className="border-b"><td className="p-3"><Link className="font-medium text-blue-600" href={`/hr/probation/${record.employee.id}`}>{record.employee.full_name}</Link><p className="text-slate-500">{record.employee.position_name}</p></td><td className="p-3">{record.employee.department?.name}</td><td className="p-3">{record.day30_due}</td><td className="p-3">{record.status}</td></tr>)}</tbody></table></div></AuthenticatedLayout>; }
