import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Badge, Button, Card, EmptyState, PageHeader } from '@/Components/shared/ui';
import { formatDate, Paginated, rows } from '@/lib/recruitment';
import { InAppNotification } from '@/types';
import { Head, router } from '@inertiajs/react';
export default function NotificationsIndex({ notifications }: { notifications: Paginated<InAppNotification> }): JSX.Element { return <AuthenticatedLayout header={<h1 className="text-lg font-semibold">Notifikasi</h1>}><Head title="Notifikasi" /><PageHeader title="Notifikasi" description="Daftar notifikasi in-app recruitment system." actions={<Button onClick={() => router.post('/notifications/read-all')}>Tandai Semua Dibaca</Button>} /><Card className="overflow-hidden"><div className="divide-y">{rows(notifications).map((notification) => <button key={notification.id} type="button" onClick={() => router.post(`/notifications/${notification.id}/read`)} className="block w-full p-4 text-left hover:bg-slate-50"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><p className="font-semibold text-slate-900">{notification.title}</p>{!notification.read_at && <Badge tone="red">Belum Dibaca</Badge>}</div><p className="mt-1 line-clamp-2 text-sm text-slate-600">{notification.body}</p><p className="mt-2 text-xs text-slate-500">{formatDate(notification.created_at)}</p></div></div></button>)}</div>{rows(notifications).length === 0 && <div className="p-4"><EmptyState title="Belum ada notifikasi" /></div>}</Card></AuthenticatedLayout>; }
