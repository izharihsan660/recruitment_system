@extends('layouts.portal')
@section('title', 'Reset Password Kandidat — '.($portalCompanyProfile?->company_name ?: config('app.name')))
@section('content')
<section class="mx-auto max-w-md px-4 py-16"><div class="rounded-xl border bg-white p-6 shadow-sm"><h1 class="text-2xl font-bold">Reset Password</h1><form method="POST" action="{{ route('candidate.password.update') }}" class="mt-6 space-y-4">@csrf<input type="hidden" name="token" value="{{ $token }}"><div><label class="text-sm font-medium">Email</label><input type="email" name="email" value="{{ old('email', $email) }}" class="mt-1 w-full rounded-md border-slate-300" required>@error('email')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-medium">Password Baru</label><input type="password" name="password" class="mt-1 w-full rounded-md border-slate-300" required>@error('password')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-medium">Konfirmasi Password</label><input type="password" name="password_confirmation" class="mt-1 w-full rounded-md border-slate-300" required></div><button class="w-full rounded-md bg-blue-600 px-4 py-2 font-semibold text-white">Reset Password</button></form></div></section>
@endsection
